<%- include("includes/head") %>
<%- include("includes/nav") %>

<div class="container mt-3">
  <div class="row">
    <div class="col-lg-2"></div>
    <div class="col-lg-8">
      <h1 class="text-center">
        Agenda      </h1>

        <p class="text-center lead">Seus contatos estão abaixo:</p>

        <%- include("includes/messages")%>
      
        <div class="responsiv-table">
          <table class="table my-3">
            <%if(contatos.length > 0){contatos.forEach((contate) => { console.log(contate._id)%>
            <tr>
              <td><%=contate.nome%></td>
    
              <td><%= contate.telefone%> </td>
              <td><%= contate.email%></td>
              <% let id = contate._id%>
              <td><a href="/contato/index/<%=id%>">Editar</a></td>
              <td><a class="text-danger"href="/contato/delete/<%=id%>">Apagar</a></td>
            </tr>
           <%})}else{%>
            <tr><td>Não foram encontrados contatos... que tal <a href="/contato/index">criar</a> um?</td></tr>
            <%}%>
          </table>
        </div>
    </div>
    <div class="col-lg-2"></div>

  </div>
</div>

  <%- include("includes/footer") %>


